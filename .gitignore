node_modules
dist
test_results*.txt
*.log
*.vtt
.DS_Store
Thumbs.db
nul
.env
.env.*
coverage
.nyc_output
*.tgz

# Cache files (regenerated at runtime)
**/cache/filter-cache.json
# Legacy project-local cache (server now uses OS temp dir instead)
.cache/

# Build artifacts
# (lib/esm is intentionally committed — it ships in the npm package)

# Test artifacts
dhurandhar_*.md
moviesdrive_*.md
test_*.md

# Runtime output artifacts (screenshots, PDFs, downloads)
downloads/
*.pdf
*.png
*.jpeg
*.jpg
.agents/

# Local tooling state (Freebuff desktop database)
.freebuff/

